apiVersion: v1
kind: Service
metadata:
  name: {{ template "helm-hello-app.fullname" . }}
  labels:
    app: {{ template "helm-hello-app.name" . }}
    chart: {{ template "helm-hello-app.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "helm-hello-app.name" . }}
    release: {{ .Release.Name }}
